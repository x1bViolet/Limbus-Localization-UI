<ResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:Icons="clr-namespace:Visual_Studio_controls.Icons">

	<SolidColorBrush x:Key="TextBox.Foreground" Color="#d6d6d6" />

	<SolidColorBrush x:Key="TextBox.Static.Border" Color="#424242" />
	<SolidColorBrush x:Key="TextBox.Static.Background" Color="#383838" />

	<SolidColorBrush x:Key="TextBox.MouseOver.Border" Color="#707070" />
	<SolidColorBrush x:Key="TextBox.MouseOver.Background" Color="#383838" />

	<SolidColorBrush x:Key="TextBox.Focus.Border" Color="#999999" />
	<SolidColorBrush x:Key="TextBox.Focus.Background" Color="#1f1f1f" />


	<Style TargetType="{x:Type TextBox}">
		<Setter Property="Background" Value="{DynamicResource TextBox.Static.Background}" />
		<Setter Property="BorderBrush" Value="{DynamicResource TextBox.Static.Border}" />
		<Setter Property="Foreground" Value="{DynamicResource TextBox.Foreground}" />
		<Setter Property="SelectionBrush" Value="{DynamicResource TextBox.Foreground}" />
		<Setter Property="CaretBrush" Value="{DynamicResource TextBox.Foreground}" />
		<Setter Property="BorderThickness" Value="1" />
		<Setter Property="KeyboardNavigation.TabNavigation" Value="None" />
		<Setter Property="HorizontalContentAlignment" Value="Left" />
		<Setter Property="FocusVisualStyle" Value="{x:Null}" />
		<Setter Property="AllowDrop" Value="true" />
		<Setter Property="ScrollViewer.PanningMode" Value="VerticalFirst" />
		<Setter Property="Stylus.IsFlicksEnabled" Value="False" />
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type TextBox}">
					<Border
						x:Name="border"
						Background="{TemplateBinding Background}"
						BorderBrush="{TemplateBinding BorderBrush}"
						BorderThickness="{TemplateBinding BorderThickness}"
						SnapsToDevicePixels="True">
						<ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden" />
					</Border>
					<ControlTemplate.Triggers>
						<Trigger Property="IsEnabled" Value="false">
							<Setter TargetName="border" Property="Background" Value="#333333" />
							<Setter TargetName="border" Property="BorderBrush" Value="#3d3d3d" />
						</Trigger>
						<Trigger Property="IsMouseOver" Value="true">
							<Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource TextBox.MouseOver.Border}" />
							<Setter TargetName="border" Property="Background" Value="{DynamicResource TextBox.MouseOver.Background}" />
						</Trigger>
						<Trigger Property="IsFocused" Value="true">
							<Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource TextBox.Focus.Border}" />
							<Setter TargetName="border" Property="Background" Value="{DynamicResource TextBox.Focus.Background}" />
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
		<Setter Property="ContextMenu">
			<Setter.Value>
				<ContextMenu>
					<ContextMenu.Items>
						<MenuItem FontSize="16.5" Command="ApplicationCommands.Copy">
							<MenuItem.Icon>
								<Icons:Copy/>
							</MenuItem.Icon>
							<MenuItem.Header>
								<TextBlock Width="100" Foreground="#d6d6d6" Text="Copy" />
							</MenuItem.Header>
						</MenuItem>
						<MenuItem FontSize="16.5" Command="ApplicationCommands.Cut">
							<MenuItem.Icon>
								<Icons:Cut/>
							</MenuItem.Icon>
							<MenuItem.Header>
								<TextBlock Width="100" Foreground="#d6d6d6" Text="Cut" />
							</MenuItem.Header>
						</MenuItem>
						<MenuItem FontSize="16.5" Command="ApplicationCommands.Paste">
							<MenuItem.Icon>
								<Icons:Paste/>
							</MenuItem.Icon>
							<MenuItem.Header>
								<TextBlock Width="100" Foreground="#d6d6d6" Text="Paste" />
							</MenuItem.Header>
						</MenuItem>
					</ContextMenu.Items>
				</ContextMenu>
			</Setter.Value>
		</Setter>
		<Style.Triggers>
			<MultiTrigger>
				<MultiTrigger.Conditions>
					<Condition Property="IsInactiveSelectionHighlightEnabled" Value="true" />
					<Condition Property="IsSelectionActive" Value="false" />
				</MultiTrigger.Conditions>
				<Setter Property="SelectionBrush" Value="{DynamicResource {x:Static SystemColors.InactiveSelectionHighlightBrushKey}}" />
			</MultiTrigger>
			<Trigger Property="IsEnabled" Value="False">
				<Setter Property="Foreground" Value="#5c5c5c" />
				<Setter Property="VerticalScrollBarVisibility" Value="Disabled" />
				<Setter Property="HorizontalScrollBarVisibility" Value="Disabled" />
			</Trigger>
		</Style.Triggers>
	</Style>
</ResourceDictionary>