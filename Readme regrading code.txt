`ref struct @Name` used to emit 'special' static classes with namespace role because their color are different from classes in my VS theme.

All `INotifyPropertyChanged` goes through `PropertyChanged.Fody` NuGet package, `LCLocalizationInterface.Explicit` abstract record.